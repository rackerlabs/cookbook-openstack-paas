#!/bin/sh
exec 2>&1

cd <%= @options[:home] %>
. <%= @options[:home] %>/bin/activate
exec chpst -u <%= @options[:user] %>:<%= @options[:group] %> env HOME=<%= @options[:homedir] %> <%= @options[:home]%>/bin/solum-worker
